# Day 2 problem description:
# Each line is a combination of A/B/C and X/Y/Z. First one is the opponent's play.
# Second one is your pick. Each row is a number of points, calculated so:
# 1/2/3 points for playing X/Y/Z respectively, and 6/3/0 points for W/D/L.

fileHandle = open("inputDay2.txt", "r");

linesArray = readlines(fileHandle);

for currentRow = eachindex(linesArray)

end
